[package]
name = "halide-build"
version = "0.2.0"
authors = ["Zach Shipko <zachshipko@gmail.com>"]
edition = "2018"

[[bin]]
name = "halide"
required-features = ["bin"]

[dependencies]
clap = {version = "2", optional = true}

[features]
default = []
bin = ["clap"]
